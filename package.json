{
  "name": "agent-switchboard",
  "version": "0.1.0",
  "description": "Mailbox-first messaging between agent sessions on one machine, with a kitty tab launcher.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --import ./tests/helpers/isolate-setup.mjs --test tests/*.test.mjs && bash tests/kitty-send.test.sh"
  },
  "license": "MIT"
}
